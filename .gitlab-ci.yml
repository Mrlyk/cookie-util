stages:
  - install
  - publish

cache: # 缓存
  paths: 
    - node_modules
    - dist

install-job:
  tags:
    - tx_linux
  stage: install
  script:
    - npm install

publish-job:
  tags:
    - tx_linux
  stage: publish
  script:
    - npm publish --registry http://110.42.187.35:4873