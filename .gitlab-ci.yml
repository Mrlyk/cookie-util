stages:
  - install
  - publish

cache: # 缓存
  paths: 
    - node_modules
    - dist

install-job:
  tags:
    - mrlyk_mac
  stage: install
  script:
    - npm install

publish-job:
  tags:
    - mrlyk_mac
  stage: publish
  script:
    - npm publish --registry http://110.42.187.35